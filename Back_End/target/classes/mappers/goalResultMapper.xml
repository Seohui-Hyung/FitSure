<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.pretzero.fitsure.model.dao.GoalResultDao">
  
  
  	<insert id="doneGoal" parameterType="GoalResult">
    	INSERT INTO goal_result (goal_id, user_id, walking, attachment, achieve, achieve_date)
    	VALUES (#{goalId}, #{userId}, #{walking}, #{attachment}, true, NOW())
	</insert>
  
  
  
  
  
  
  </mapper>