<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.pretzero.fitsure.model.dao.UserDao">
  
  		<select id="selectOne" parameterType = "Map" resultType="User">
			SELECT user_loginId, password 
			FROM user 
			WHERE user_loginId = #{userLoginId} AND password  = #{password}
		</select>
  
  
  		<select id="checkValidEmail" parameterType = "String" resultType="Int">
			SELECT count(*)
			FROM user 
			WHERE user_email = #{Email}
		</select>
  		
  		<insert id="insertUser" parameterType="User" >
  			INSERT INTO user (user_loginid, password, username, email, birth_date, gender)
  			VALUES (#{userLoginId}, #{password}, #{username}, #{email}, #{birthDate}, #{gender})
  			
  		
  		</insert>
  
  
  
  </mapper>
  